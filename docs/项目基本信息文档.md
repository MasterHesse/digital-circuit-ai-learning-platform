# DigLearn（数字电路 AI 学习平台）— 项目结构与接口参考文档

> 最后更新：2026-02-23

---

## 1. 仓库总体结构（Repository Layout）

```bash
.
├── README.md
├── backend
│   ├── mvnw
│   ├── mvnw.cmd
│   ├── pom.xml
│   └── src
│       ├── main
│       │   ├── java
│       │   │   └── dev
│       │   │       └── masterhesse
│       │   │           └── diglearn
│       │   │               ├── DigLearnBackendApplication.java
│       │   │               ├── auth
│       │   │               │   ├── AppUserDetailsService.java
│       │   │               │   ├── AuthApiModels.java
│       │   │               │   ├── AuthController.java
│       │   │               │   ├── AuthNormalization.java
│       │   │               │   ├── AuthService.java
│       │   │               │   └── UserPrincipal.java
│       │   │               ├── classroom
│       │   │               │   ├── ClassApiModels.java
│       │   │               │   ├── ClassroomController.java
│       │   │               │   ├── ClassroomService.java
│       │   │               │   └── persistence
│       │   │               │       ├── ClassMembershipEntity.java
│       │   │               │       ├── ClassMembershipRepository.java
│       │   │               │       ├── ClassMembershipStatus.java
│       │   │               │       ├── ClassroomEntity.java
│       │   │               │       └── ClassroomRepository.java
│       │   │               ├── config
│       │   │               │   ├── SecurityConfig.java
│       │   │               │   └── TransactionManagersConfig.java
│       │   │               ├── knowledgepoint
│       │   │               │   ├── KnowledgePoint.java
│       │   │               │   ├── KnowledgePointController.java
│       │   │               │   ├── KnowledgePointRepository.java
│       │   │               │   ├── application
│       │   │               │   │   └── KpGraphService.java
│       │   │               │   └── dto
│       │   │               │       └── PrereqDto.java
│       │   │               ├── practice
│       │   │               │   ├── api
│       │   │               │   │   └── PracticeController.java
│       │   │               │   ├── application
│       │   │               │   │   ├── PracticeService.java
│       │   │               │   │   └── QuestionSubmissionService.java
│       │   │               │   ├── domain
│       │   │               │   │   └── PracticeMode.java
│       │   │               │   └── persistence
│       │   │               │       ├── QuestionAttemptEntity.java
│       │   │               │       ├── QuestionAttemptRepository.java
│       │   │               │       ├── UserQuestionStateEntity.java
│       │   │               │       └── UserQuestionStateRepository.java
│       │   │               ├── question
│       │   │               │   ├── api
│       │   │               │   │   ├── QuestionController.java
│       │   │               │   │   ├── TagController.java
│       │   │               │   │   └── TagKpMapController.java
│       │   │               │   ├── application
│       │   │               │   │   ├── TagKpMapService.java
│       │   │               │   │   └── TagService.java
│       │   │               │   ├── domain
│       │   │               │   │   ├── QuestionPool.java
│       │   │               │   │   ├── QuestionStatus.java
│       │   │               │   │   └── QuestionType.java
│       │   │               │   └── persistence
│       │   │               │       ├── QuestionEntity.java
│       │   │               │       ├── QuestionRepository.java
│       │   │               │       ├── QuestionTagMapEntity.java
│       │   │               │       ├── QuestionTagMapId.java
│       │   │               │       ├── QuestionTagMapRepository.java
│       │   │               │       ├── TagEntity.java
│       │   │               │       ├── TagKpMapEntity.java
│       │   │               │       ├── TagKpMapId.java
│       │   │               │       ├── TagKpMapRepository.java
│       │   │               │       └── TagRepository.java
│       │   │               ├── shared
│       │   │               │   └── application
│       │   │               │       └── UserGuard.java
│       │   │               ├── sim
│       │   │               │   ├── api
│       │   │               │   │   ├── LevelApiModels.java
│       │   │               │   │   ├── LevelController.java
│       │   │               │   │   ├── LevelJudgeApiModels.java
│       │   │               │   │   └── LevelJudgeController.java
│       │   │               │   ├── application
│       │   │               │   │   ├── LevelJudgeService.java
│       │   │               │   │   └── LevelQueryService.java
│       │   │               │   ├── importing
│       │   │               │   │   ├── LevelImportModels.java
│       │   │               │   │   ├── LevelImportRunner.java
│       │   │               │   │   ├── LevelImportService.java
│       │   │               │   │   └── LevelsImportProperties.java
│       │   │               │   ├── judge
│       │   │               │   │   ├── CircuitSpec.java
│       │   │               │   │   ├── CircuitValidator.java
│       │   │               │   │   ├── IterativeBooleanJudgeEvaluator.java
│       │   │               │   │   └── SimpleBooleanJudgeEvaluator.java
│       │   │               │   └── persistence
│       │   │               │       ├── LevelEntity.java
│       │   │               │       ├── LevelPassRecordEntity.java
│       │   │               │       ├── LevelPassRecordRepository.java
│       │   │               │       ├── LevelRepository.java
│       │   │               │       ├── LevelTestCaseEntity.java
│       │   │               │       ├── LevelTestCaseRepository.java
│       │   │               │       └── LevelTestStepEntity.java
│       │   │               ├── user
│       │   │               │   ├── AdminUserController.java
│       │   │               │   ├── UserApiModels.java
│       │   │               │   ├── UserRole.java
│       │   │               │   ├── UserService.java
│       │   │               │   ├── api
│       │   │               │   │   ├── TeacherRequestAdminController.java
│       │   │               │   │   └── UserController.java
│       │   │               │   └── persistence
│       │   │               │       ├── AppUserEntity.java
│       │   │               │       ├── AppUserRepository.java
│       │   │               │       ├── TeacherRequestEntity.java
│       │   │               │       ├── TeacherRequestRepository.java
│       │   │               │       └── TeacherRequestStatus.java
│       │   │               └── web
│       │   │                   └── GlobalExceptionHandler.java
│       │   └── resources
│       │       ├── application.yml
│       │       ├── static
│       │       └── templates
│       └── test
│           └── java
│               └── dev
│                   └── masterhesse
│                       └── diglearn
│                           └── DigLearnBackendApplicationTests.java
├── docs
│   ├── seed_digital_circuits_basic.cypher
│   ├── 自定义方法说明.md
│   └── 项目基本信息文档.md
├── frontend
│   ├── index.html
│   ├── package-lock.json
│   ├── package.json
│   ├── public
│   │   ├── hdu.png
│   │   └── vite.svg
│   ├── src
│   │   ├── App.vue
│   │   ├── assets
│   │   │   └── vue.svg
│   │   ├── components
│   │   │   └── AppNavBar.vue
│   │   ├── lib
│   │   │   └── api.ts
│   │   ├── main.js
│   │   ├── pages
│   │   │   ├── ClassDetail.vue
│   │   │   ├── Classes.vue
│   │   │   ├── Login.vue
│   │   │   ├── OnlineVerilogEditorPage.vue
│   │   │   ├── Practice.vue
│   │   │   ├── Profile.vue
│   │   │   ├── Promote.vue
│   │   │   └── Register.vue
│   │   ├── router
│   │   │   └── index.ts
│   │   ├── stores
│   │   │   └── session.ts
│   │   └── style.css
│   └── vite.config.js
└── infra
    ├── docker-compose.yml
    └── neo4j
        └── import  [error opening dir]
```

---

## 2. 后端工程信息与依赖（pom.xml）

**项目基本信息（来自 `pom.xml`）**

- Spring Boot Parent：`org.springframework.boot:spring-boot-starter-parent:3.5.10`
- Java 版本：`21`
- GroupId：`dev.masterhesse`
- ArtifactId：`diglearn-backend`
- Version：`0.0.1-SNAPSHOT`
- 项目名（name）：`DigLearn Backend`
- 项目描述（description）：`Backend service for digital circuit AI learning platform`

**依赖（Dependencies）**

- Web / API
  - `spring-boot-starter-web`
  - `spring-boot-starter-validation`
- 安全
  - `spring-boot-starter-security`
- 运维 / 监控
  - `spring-boot-starter-actuator`
- Neo4j
  - `spring-boot-starter-data-neo4j`
- API 文档（OpenAPI / Swagger UI）
  - `org.springdoc:springdoc-openapi-starter-webmvc-ui:2.8.0`
- PostgreSQL（JPA）
  - `spring-boot-starter-data-jpa`
  - `org.postgresql:postgresql`（scope：runtime）
- Lombok
  - `org.projectlombok:lombok:1.18.34`（scope：provided）
- 测试
  - `spring-boot-starter-test`（scope：test）
  - `spring-security-test`（scope：test）
---

## 3. 配置与账号密码（Config）

### 3.1 backend `application.yml`

路径：`backend/src/main/resources/application.yml`

当前关键信息：

```yaml
server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui
  api-docs:
    path: /v3/api-docs

spring:
    neo4j:
        uri: bolt://localhost:7687
        authentication:
            username: neo4j
            password: neo4j_pwd
    datasource:
        url: jdbc:postgresql://localhost:5433/diglearn
        username: postgres
        password: 123456
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                format_sql: true
        open-in-view: false
```

说明：

- 后端端口：`8080`
- 前端端口：`5173`
- Actuator：`/actuator/health`、`/actuator/info`
- Swagger：
  - UI：`/swagger-ui`
  - OpenAPI JSON：`/v3/api-docs`
- Neo4j：
  - Bolt：`bolt://localhost:7687`
  - 用户名/密码：`neo4j / neo4j_pwd`
- PostgreSQL（JPA）：
  - `jdbc:postgresql://localhost:5433/diglearn`
  - `postgres / 123456`
  - `spring.jpa.hibernate.ddl-auto: update`（启动时自动建表/更新表结构）
  - `open-in-view: false`
---

### 3.2 infra `docker-compose.yml`

路径：`infra/docker-compose.yml`

当前内容：

```yaml
services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: diglearn-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5433:5432"
    volumes:
      - diglearn_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 20

  neo4j:
    image: neo4j:5
    container_name: diglearn-neo4j
    environment:
      NEO4J_AUTH: ${NEO4J_AUTH}
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - diglearn_neo4j_data:/data
      - diglearn_neo4j_logs:/logs
      - ./neo4j/import:/var/lib/neo4j/import
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:7474 >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30

  redis:
    image: redis:7-alpine
    container_name: diglearn-redis
    ports:
      - "6380:6379"
    volumes:
      - diglearn_redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  rabbitmq:
    image: rabbitmq:3-management
    container_name: diglearn-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS}
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - diglearn_rabbitmq:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD-SHELL", "rabbitmq-diagnostics -q ping"]
      interval: 10s
      timeout: 5s
      retries: 30

volumes:
  diglearn_postgres:
  diglearn_neo4j_data:
  diglearn_neo4j_logs:
  diglearn_redis:
  diglearn_rabbitmq:
```

说明：

- Neo4j 容器：
  - Web UI：`7474`
  - Bolt：`7687`
  - 导入目录挂载：`infra/neo4j/import -> /var/lib/neo4j/import`
- RabbitMQ 带管理面板：`15672`
- Postgres 使用 `pgvector` 镜像（pg16）
- Redis 7

---

### 3.3 infra `.env`

路径：`infra/.env`

当前内容：

```dotenv
POSTGRES_DB=diglearn
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123456

NEO4J_AUTH=neo4j/neo4j_pwd

RABBITMQ_DEFAULT_USER=diglearn
RABBITMQ_DEFAULT_PASS=123456
```

说明：

- Postgres：`postgres / 123456`，数据库 `diglearn`
- Neo4j：`neo4j / neo4j_pwd`（通过 `NEO4J_AUTH` 注入容器）
- RabbitMQ：`diglearn / 123456`

---

### 3.4 端口与地址速查表

| 组件 | 地址/端口 | 用途 |
|---|---|---|
| Backend | `http://localhost:8080` | 业务 API |
| Swagger UI | `http://localhost:8080/swagger-ui` | API 文档 |
| OpenAPI JSON | `http://localhost:8080/v3/api-docs` | 文档源 |
| Actuator Health | `http://localhost:8080/actuator/health` | 健康检查 |
| Neo4j Browser | `http://localhost:7474` | 图数据库 Web 控制台 |
| Neo4j Bolt | `bolt://localhost:7687` | 后端连接用 |
| Postgres（宿主机） | `localhost:5433` | Circuit 存档（JPA） |
| Redis | `localhost:6380` | 预留/后续使用 |
| RabbitMQ AMQP | `localhost:5672` | 预留/后续使用 |
| RabbitMQ 管理台 | `http://localhost:15672` | 管理界面 |
| Frontend（Vite 默认） | `http://localhost:5173` | 前端开发服务器（默认端口） |

---


## 4. 数据模型（Data Model）

### 4.1 Neo4j 图模型（知识点）

**节点 Label：** `KnowledgePoint`

字段（对应 Java 实体 `KnowledgePoint`）：

- `kpId: String`（唯一 ID，`@Id`）
- `title: String`
- `category: String`
- `difficulty: Integer`

**关系类型：** `:PREREQ`

方向约定：

- `(A:KnowledgePoint)-[:PREREQ]->(B:KnowledgePoint)`
- 含义：A 的前置知识点是 B（A 依赖 B）

---

### 4.2 PostgreSQL

#### 电路仿真存档

##### 表名：`levels`
对应实体：`LevelEntity`

- **主键（PK）**
  - `code` varchar(64) NOT NULL

- **字段**
  - `title` varchar(200) NOT NULL
  - `description` text NULL
  - `template_circuit_json` text NULL
  - `devices` text NULL
  - `allow_cycles` boolean NOT NULL

- **关联/子表**
  - `level_allowed_components`（来自 `allowedComponents`：`@ElementCollection`）
  - `level_kp_ids`（来自 `kpIds`：`@ElementCollection`）
  - `level_test_cases`（来自 `testCases`：`@OneToMany`）

---

##### 表名：`level_allowed_components`
对应实体字段：`LevelEntity.allowedComponents`

- **字段**
  - `level_code` varchar(64) NOT NULL（建议外键 FK -> `levels(code)`；JPA `joinColumns` 指向 `code`）
  - `component` varchar(64) NOT NULL

---

##### 表名：`level_kp_ids`
对应实体字段：`LevelEntity.kpIds`

- **字段**
  - `level_code` varchar(64) NOT NULL（建议外键 FK -> `levels(code)`）
  - `kp_id` varchar(64) NOT NULL

---

##### 表名：`level_test_cases`
对应实体：`LevelTestCaseEntity`

- **主键（PK）**
  - `id` uuid NOT NULL（`@GeneratedValue(strategy = UUID)`）

- **外键（FK）**
  - `level_code` NOT NULL（`@JoinColumn(name="level_code")`，关联 `levels(code)`）

- **字段**
  - `order_index` int NOT NULL
  - `name` varchar(200) NULL

- **唯一约束（UK）**
  - `uk_level_test_case_order`：(`level_code`, `order_index`)

- **关联/子表**
  - `level_test_steps`（`@OneToMany(mappedBy="testCase")`，按 `step_index` 升序）

---

##### 表名：`level_test_steps`
对应实体：`LevelTestStepEntity`

- **主键（PK）**
  - `id` uuid NOT NULL（`@GeneratedValue(strategy = UUID)`）

- **外键（FK）**
  - `test_case_id` uuid NOT NULL（`@JoinColumn(name="test_case_id")`，关联 `level_test_cases(id)`）

- **字段**
  - `step_index` int NOT NULL
  - `inputs_json` text NOT NULL
  - `expected_json` text NOT NULL

- **唯一约束（UK）**
  - `uk_test_step_index`：(`test_case_id`, `step_index`)

---

##### 表名：`level_pass_records`
对应实体：`LevelPassRecordEntity`

- **主键（PK）**
  - `id` bigint NOT NULL（`@GeneratedValue(strategy = IDENTITY)`）

- **字段**
  - `user_id` varchar(128) NOT NULL
  - `level_code` varchar(64) NOT NULL
  - `first_passed_at` timestamptz NOT NULL（Java `Instant`）
  - `last_passed_at` timestamptz NOT NULL（Java `Instant`）
  - `pass_count` int NOT NULL

- **唯一约束（UK）**
  - `uk_level_pass_user_level`：(`user_id`, `level_code`)

---

#### 题库与 Tags 存档

##### 表名：`questions`
对应实体：`QuestionEntity`

- **主键（PK）**
  - `id` uuid NOT NULL（`@PrePersist` 自动生成）

- **字段**
  - `type` varchar(40) NOT NULL（枚举 `QuestionType`，`EnumType.STRING`）
  - `status` varchar(20) NOT NULL（枚举 `QuestionStatus`，`EnumType.STRING`，默认 `DRAFT`）
  - `stem` text NOT NULL
  - `difficulty` smallint NOT NULL（取值范围 1..5）
  - `lang` varchar(10) NOT NULL（默认 `zh-CN`）
  - `content` jsonb NOT NULL
  - `solution` jsonb NULL
  - `explanation` text NULL
  - `created_at` timestamptz NOT NULL（Java `Instant`，`@PrePersist` 写入）
  - `updated_at` timestamptz NOT NULL（Java `Instant`，`@PrePersist` / `@PreUpdate` 写入）
  - `question_pool`	varchar(20)	NOT NULL	（枚举 `QuestionPool`（CHAPTER / SUPPLEMENT），`EnumType.STRING`）

- **索引**
  - `idx_questions_type`：(`type`)
  - `idx_questions_status`：(`status`)
  - `idx_questions_difficulty`：(`difficulty`)

---

##### 表名：`tags`
对应实体：`TagEntity`

- **主键（PK）**
  - `id` uuid NOT NULL（`@GeneratedValue(strategy = GenerationType.UUID)`）

- **字段**
  - `name` varchar(64) NOT NULL（建议存规范化形式，如大写 + 下划线）
  - `description` text NULL

- **唯一约束（UK）**
  - `uk_tags_name`：(`name`)

---

##### 表名：`question_tag_map`
对应实体：`QuestionTagMapEntity`（复合主键 `QuestionTagMapId`）

- **主键（PK，嵌入式复合键）**
  - `question_id` uuid NOT NULL
  - `tag_id` uuid NOT NULL

- **字段**
  - `weight` int NOT NULL（默认 1，`@PrePersist` 校正 ≤0 时置为 1）
  - `created_at` timestamptz NOT NULL（Java `Instant`，`@PrePersist` 写入）

- **唯一约束（UK）**
  - `uk_question_tag`：(`question_id`, `tag_id`)

- **索引**
  - `idx_qtm_tag_id`：(`tag_id`)

---

##### 表名：`tag_kp_map`
对应实体：`TagKpMapEntity`（复合主键 `TagKpMapId`）

- **主键（PK，嵌入式复合键）**
  - `tag_id` uuid NOT NULL
  - `kp_id` varchar(64) NOT NULL

- **外键（FK）**
  - `tag_id` → `tags(id)`（`@ManyToOne(fetch = LAZY)`，`fk_tag_kp_map_tag`）

- **字段**
  - `weight` int NOT NULL

- **唯一约束（UK）**
  - `uk_tag_kp_map_tag_kp`：(`tag_id`, `kp_id`)

- **索引**
  - `idx_tag_kp_map_tag_id`：(`tag_id`)
  - `idx_tag_kp_map_kp_id`：(`kp_id`)

---

##### 表名：`question_attempts`
对应实体：`QuestionAttemptEntity`

- **主键（PK）**
  - `id` uuid NOT NULL（`@PrePersist` 自动生成）

- **字段**
  - `user_id` varchar(128) NOT NULL
  - `question_id` uuid NOT NULL
  - `mode` varchar(20) NOT NULL（枚举 `PracticeMode`，`EnumType.STRING`）
  - `context_kp_id` varchar(64) NULL（章节练习上下文，用于统计/展示）
  - `source_question_id` uuid NULL（巩固练习：由哪一道错题触发）
  - `user_answer` jsonb NOT NULL
  - `is_correct` boolean NULL
  - `submitted_at` timestamptz NOT NULL（Java `Instant`，`@PrePersist` 写入）

- **索引**
  - `idx_attempt_user_question`：(`user_id`, `question_id`)
  - `idx_attempt_user_mode`：(`user_id`, `mode`)

---

##### 表名：`user_question_state`
对应实体：`UserQuestionStateEntity`

- **主键（PK）**
  - `id` uuid NOT NULL（`@PrePersist` 自动生成）

- **字段**
  - `user_id` varchar(128) NOT NULL
  - `question_id` uuid NOT NULL
  - `mastered` boolean NOT NULL（默认 `false`）
  - `wrong_count` int NOT NULL（默认 `0`）
  - `last_wrong_at` timestamptz NULL（Java `Instant`）
  - `last_attempt_at` timestamptz NULL（Java `Instant`）

- **唯一约束（UK）**
  - `uk_user_question_state`：(`user_id`, `question_id`)

- **索引**
  - `idx_uqs_user_mastered`：(`user_id`, `mastered`)

---

#### 用户管理存档

##### 表名：`app_users`
对应实体：`AppUserEntity`

- **主键（PK）**
  - `user_id` varchar(128) NOT NULL

- **字段**
  - `username` varchar(64) NOT NULL（存规范化后的：小写、去掉前导@）
  - `email` varchar(254) NOT NULL（建议存小写）
  - `password_hash` varchar(255) NOT NULL
  - `role` varchar(20) NOT NULL（Java `UserRole` 枚举）
  - `name` varchar(128) NOT NULL
  - `enabled` boolean NOT NULL（默认 `true`）

- **唯一约束（UK）**
  - `uk_app_users_username`：(`username`)
  - `uk_app_users_email`：(`email`)

---

##### 表名：`teacher_requests`
对应实体：`TeacherRequestEntity`

- **主键（PK）**
  - `user_id` varchar(128) NOT NULL

- **字段**
  - `status` varchar(16) NOT NULL（Java `TeacherRequestStatus` 枚举）
  - `requested_at` timestamptz NOT NULL（Java `Instant`）
  - `decided_at` timestamptz NULL（Java `Instant`）

---

#### 班级管理

##### 表名：`classes`
对应实体：`ClassroomEntity`

- **主键（PK）**
  - `id` uuid NOT NULL（`@PrePersist` 自动生成）

- **字段**
  - `name` varchar(200) NOT NULL
  - `teacher_id` varchar(128) NOT NULL
  - `created_at` timestamptz NOT NULL（Java `Instant`，`@PrePersist` 自动生成）

- **索引**
  - `idx_classes_teacher`：(`teacher_id`)

---

##### 表名：`class_membership`
对应实体：`ClassMembershipEntity`

- **主键（PK）**
  - `id` uuid NOT NULL（`@PrePersist` 自动生成）

- **字段**
  - `class_id` uuid NOT NULL
  - `student_id` varchar(128) NOT NULL
  - `status` varchar(16) NOT NULL（默认 `PENDING`，Java `ClassMembershipStatus` 枚举）
  - `requested_at` timestamptz NOT NULL（Java `Instant`，`@PrePersist` 自动生成）
  - `decided_at` timestamptz NULL（Java `Instant`）
  - `decided_by` varchar(128) NULL

- **唯一约束（UK）**
  - `uk_class_membership`：(`class_id`, `student_id`)

- **索引**
  - `idx_cm_class_status`：(`class_id`, `status`)
  - `idx_cm_student_status`：(`student_id`, `status`)

---

## 5. 后端代码结构

包名根：`dev.masterhesse.diglearn`

---
### 5.1 启动入口
- `DigLearnBackendApplication.java`：Spring Boot 启动类

---
### 5.2 config（全局配置）
- `SecurityConfig.java`：Spring Security 配置（认证/授权、过滤器链等）
- `TransactionManagersConfig.java`：事务管理器配置（用于处理 JPA 与 Neo4j 多数据源的事务管理）

---
### 5.3 auth（认证与授权）
- `AuthApiModels.java`：认证相关 API 的请求/响应模型（登录、注册 DTO）
- `AuthController.java`：认证对外 REST API（如 `/api/auth/login` 等）
- `AuthNormalization.java`：认证信息标准化工具（如用户名转小写、去空格等）
- `AuthService.java`：认证核心业务逻辑
- `AppUserDetailsService.java`：Spring Security 的 UserDetailsService 实现，用于加载用户信息
- `UserPrincipal.java`：Spring Security 的认证主体（封装当前登录用户信息）

---
### 5.4 knowledgepoint（知识点图谱 - Neo4j）
- `KnowledgePoint.java`：知识点图模型（节点/关系实体）
- `KnowledgePointController.java`：知识点 REST API
- `KnowledgePointRepository.java`：Neo4j Repository + 自定义查询（如前置链）
- `application/KpGraphService.java`：知识点图谱的业务编排与复杂查询逻辑
- `dto/PrereqDto.java`：`/prereqs` 输出 DTO（包含 depth 等信息）

---
### 5.5 classroom（班级管理）
- `ClassApiModels.java`：班级相关 API 模型
- `ClassroomController.java`：班级管理对外 REST API
- `ClassroomService.java`：班级业务逻辑（创建班级、加入班级、审核等）
- **persistence（JPA 持久化层）**
  - `ClassroomEntity.java` / `ClassroomRepository.java`：班级实体与仓储
  - `ClassMembershipEntity.java` / `ClassMembershipRepository.java`：班级成员关系实体与仓储
  - `ClassMembershipStatus.java`：成员状态枚举（如 PENDING, APPROVED 等）

---
### 5.6 practice（练习模块）
- **api**
  - `PracticeController.java`：练习模式相关 REST API
- **application**
  - `PracticeService.java`：练习模式业务逻辑（如抽题、生成练习会话）
  - `QuestionSubmissionService.java`：题目提交与判题结果记录逻辑
- **domain**
  - `PracticeMode.java`：练习模式枚举（如顺序、随机、错题本等）
- **persistence（JPA 持久化层）**
  - `QuestionAttemptEntity.java` / `QuestionAttemptRepository.java`：用户单次做题尝试记录
  - `UserQuestionStateEntity.java` / `UserQuestionStateRepository.java`：用户题目掌握状态（错题次数、是否掌握等）

---
### 5.7 question（题库管理）
- **api**
  - `QuestionController.java`：题目增删改查 REST API
  - `TagController.java`：题目标签管理 REST API
  - `TagKpMapController.java`：标签与知识点映射管理 REST API
- **application**
  - `TagService.java`：标签业务逻辑
  - `TagKpMapService.java`：标签与知识点关联业务逻辑
- **domain**
  - `QuestionPool.java`：题库分类枚举/常量
  - `QuestionStatus.java`：题目状态枚举（如草稿、发布）
  - `QuestionType.java`：题型枚举（如单选、多选、编程题等）
- **persistence（JPA 持久化层）**
  - `QuestionEntity.java` / `QuestionRepository.java`：题目实体与仓储
  - `TagEntity.java` / `TagRepository.java`：标签实体与仓储
  - `QuestionTagMapEntity.java` / `QuestionTagMapId.java` / `QuestionTagMapRepository.java`：题目-标签多对多关联
  - `TagKpMapEntity.java` / `TagKpMapId.java` / `TagKpMapRepository.java`：标签-知识点多对多关联

---
### 5.8 shared（共享模块）
- `application/UserGuard.java`：通用用户权限与身份守卫（用于校验当前操作用户是否有权访问特定资源）

---
### 5.9 sim（电路仿真与关卡）
#### 5.9.1 sim.api（对外 REST API：关卡与判题）
- `LevelApiModels.java`：关卡相关 API 请求/响应模型（DTO/VO 集合）
- `LevelController.java`：关卡查询相关接口
- `LevelJudgeApiModels.java`：判题接口的请求/响应模型
- `LevelJudgeController.java`：判题相关接口

#### 5.9.2 sim.application（应用服务层）
- `LevelQueryService.java`：关卡查询/读取编排逻辑（聚合 persistence 数据）
- `LevelJudgeService.java`：判题流程编排（调用 judge 校验/评估，更新通关记录等）

#### 5.9.3 sim.judge（判题核心逻辑）
- `CircuitSpec.java`：电路规格/约束描述
- `CircuitValidator.java`：电路结构合法性校验
- `SimpleBooleanJudgeEvaluator.java`：布尔判题评估器（组合逻辑）
- `IterativeBooleanJudgeEvaluator.java`：布尔判题评估器（时序逻辑）

#### 5.9.4 sim.importing（关卡导入）
- `LevelImportModels.java`：导入用数据模型
- `LevelImportRunner.java`：启动时/任务触发的导入入口（Runner）
- `LevelImportService.java`：导入业务逻辑
- `LevelsImportProperties.java`：导入配置（properties 绑定）

#### 5.9.5 sim.persistence（关卡与通关记录：JPA 持久化层）
- `LevelEntity.java` / `LevelRepository.java`：关卡主表
- `LevelTestCaseEntity.java` / `LevelTestCaseRepository.java`：关卡测试用例
- `LevelTestStepEntity.java`：测试步骤
- `LevelPassRecordEntity.java` / `LevelPassRecordRepository.java`：通关记录

---
### 5.10 user（用户管理）
- `UserApiModels.java`：用户相关 API 模型（DTO/VO 集合）
- `UserRole.java`：用户角色枚举（如 STUDENT, TEACHER, ADMIN）
- `UserService.java`：用户业务逻辑/编排
- `AdminUserController.java`：管理员专用的用户管理接口

#### 5.10.1 user.api（对外 REST API）
- `UserController.java`：普通用户接口（查询个人信息、更新资料等）
- `TeacherRequestAdminController.java`：教师权限申请后台管理接口

#### 5.10.2 user.persistence（JPA 持久化层）
- `AppUserEntity.java` / `AppUserRepository.java`：用户实体与仓储
- `TeacherRequestEntity.java` / `TeacherRequestRepository.java`：教师权限申请记录实体与仓储
- `TeacherRequestStatus.java`：申请状态枚举

---
### 5.11 web（Web 层通用能力）
- `GlobalExceptionHandler.java`：全局异常处理（统一错误响应格式、参数校验异常等）

---

## 6. 对外 API 列表与用法
见文档 `api-docs.json`

---

## 7. 本地运行与验证

### 7.1 启动基础设施（Neo4j/Postgres/Redis/RabbitMQ）

在 `infra/` 目录：

```bash
docker compose --env-file .env up -d
docker compose ps
```

检查:
- Neo4j Browser：`http://localhost:7474`
- Postgres：宿主机 `5433`
---

### 7.2 启动后端

在 `backend/` 目录：

```bash
./mvnw spring-boot:run
```

---

### 7.3 启动前端（frontend）

在 `frontend/` 目录：

```bash
npm install
npm run dev
```

打开 Vite 地址（通常是 `http://localhost:5173`）

---

## 8. 命名规范

### 8.1 知识点 ID（kpId）

- `kpId`：知识点唯一 ID（Node 主键 / API 路径参数）
- `depth`：前置展开深度（查询参数/DTO 字段；从 1 开始）
- `category`：知识点分类/章节（字符串）
- `difficulty`：难度（整数，范围可后续定义）

示例:
```text
DC-FND-01
DC-BOOL-01
DC-SEQ-04
```

### 8.2 关卡文件（levels）

关卡以 **JSON 文件** 存放（供导入器读取），文件名与关卡 `code` 强绑定，便于排序、定位与防重复。

**命名格式（推荐/当前示例一致）**

- 文件名：`<code>.json`
- `code` 格式：`L<NN>_<SLUG>`
  - `L`：固定前缀（Level）
  - `<NN>`：两位数字序号（`01..99`），用于自然排序与展示（例如章节/关卡顺序）
  - `_`：分隔符
  - `<SLUG>`：关卡短标识，建议使用大写字母、数字与下划线（不含空格），体现主题/目标

**约束**

- 文件名（不含扩展名）必须等于 JSON 内的 `code` 字段值  
  - 例：文件 `L02_XOR.json` 内 `code` 必须为 `L02_XOR`
- `<NN>` 建议保持**递增且不重用**（便于版本迭代时稳定排序）
- `<SLUG>` 应语义明确，避免过长（建议 3～20 字符）

**示例**

```text
L01_AND.json
L02_XOR.json
L03_MUX2.json
L10_FULL_ADDER.json
```

### 8.3 题目文件

#### 单选题 SINGLE_CHOICE
```json
{
  "type": "SINGLE_CHOICE",
  "stem": "下列哪个是德摩根定律的正确形式？",
  "difficulty": 2,
  "content": {
    "options": [
      {"id": "A", "text": "(AB)'=A'+B'"},
      {"id": "B", "text": "(A+B)'=A'B'"},
      {"id": "C", "text": "A+B = AB"},
      {"id": "D", "text": "A'=A"}
    ]
  },
  "solution": {
    "correctOptionId": "B"
  },
  "explanation": "德摩根定律：$(A+B)'=A'B'$，$(AB)'=A'+B'$。"
}
```

提交答案
```json
{ "selectedOptionIds": ["A", "D"] }
```

---

#### 多选题 MULTI_CHOICE
```json
{
  "type": "MULTI_CHOICE",
  "stem": "下列哪些表达式恒等成立？（多选）",
  "difficulty": 3,
  "content": {
    "options": [
      {"id": "A", "text": "A + A = A"},
      {"id": "B", "text": "A·1 = A"},
      {"id": "C", "text": "A·0 = A"},
      {"id": "D", "text": "A + 0 = A"}
    ]
  },
  "solution": {
    "correctOptionIds": ["A", "B", "D"]
  },
  "explanation": "A·0=0，因此 C 错。"
}
```

提交答案
```json
{ "selectedOptionIds": ["A", "D"] }
```

---

#### 简答题 SHORT_ANSWER
```json
{
  "type": "SHORT_ANSWER",
  "stem": "用一句话说明什么是组合逻辑冒险（hazard）。",
  "difficulty": 2,
  "content": {
    "placeholder": "写出你的定义…",
    "maxLength": 500
  },
  "solution": {
    "referenceAnswer": "由于不同路径延迟不一致导致输出出现短暂错误脉冲的现象。",
    "keywords": ["延迟", "路径", "短暂", "毛刺"]
  },
  "explanation": "也可补充静态冒险/动态冒险等分类。"
}
```

提交答案
```json
{ "text": "..." }
```

---
